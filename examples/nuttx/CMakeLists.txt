# CMakeLists.txt — zenoh-pico NuttX examples
#
# This directory contains documentation only (README.md).
# The actual buildable source lives in the NuttX apps tree:
#
#   submodules/nuttx-apps/examples/zenoh_pub/   (zenoh_pub_main.c + CMakeLists.txt + Kconfig)
#   submodules/nuttx-apps/examples/zenoh_sub/   (zenoh_sub_main.c + CMakeLists.txt + Kconfig)
#
# Build via NuttX:
#   cd submodules/nuttx && ./tools/configure.sh t3-gem-o1:nsh && make -j$(nproc)
